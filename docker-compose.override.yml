services:
  frontend:
    volumes:
      - ./frontend:/app
    command: sh -c "npm install && npm run dev -- --host"
    environment:
      VITE_API_BASE: http://127.0.0.1:4001/api
    ports:
      - "3000:3000"
